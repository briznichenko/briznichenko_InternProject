{% include 'header' %}

#import "{{ prefix }}{{ module_info.name }}Controller.h"


@implementation {{ prefix }}{{ module_info.name }}Controller

-(instancetype) initAndAssemble
{
	self = [super init];
	if(self)
	{
		self.{{ prefix }}{{ module_info.name | downcase}}ViewController = [{{ prefix }}{{ module_info.name }}ViewController new];
		self.{{ prefix }}{{ module_info.name | downcase}}Model = [[{{ prefix }}{{ module_info.name }}Model alloc] initWithData];
	}	
	return self;
}

#pragma mark -- Routing

@end
